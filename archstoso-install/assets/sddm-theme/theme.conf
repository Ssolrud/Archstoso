[General]
description = ArchStoso SDDM Theme
type        = image

# No background image — Main.qml paints a solid Rectangle.
# SDDM falls back to color= when background= is empty.
background  =
color       = #1a110f
