# -----------------------------------------------------
# Key bindings
# Uses variation system — change preset by editing source line
# Available presets: see conf/keybindings/
# -----------------------------------------------------
source = ~/.config/hypr/conf/keybindings/default.conf
